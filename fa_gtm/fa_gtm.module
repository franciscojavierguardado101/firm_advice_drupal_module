<?php

/**
 * Implements hook_preprocess_HOOK() for block templates.
 */
function fa_gtm_preprocess_page(&$variables) {
  // Attach the library to all pages.
  $variables['#attached']['library'][] = 'fa_gtm/events';
}
